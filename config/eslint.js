module.exports = {
  // unused vars are unallowed, unused args are okay
  'no-unused-vars': [2, { args: 'none' }],

  // must use semicolons
  semi: [2, 'always'],

  // disallow dangling commas
  'comma-dangle': ["error", "never"]
};
